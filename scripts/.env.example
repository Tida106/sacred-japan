# Copy this file to .env (in the same scripts/ folder) and paste your key.
# .env is gitignored — never commit the real key.
#
# Get a key: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_new_key_here
